import React from "react";

const App: React.FC = () => {
  return (
    <div className="app">
      <h1>Welcome to React + TypeScript + Webpack</h1>
      <p>Your application is now set up!</p>
    </div>
  );
};

export default App;
